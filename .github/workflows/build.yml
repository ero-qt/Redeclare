name: build

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # MinVer reads the tags

      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      # Warnings are errors and code style is enforced in the build, so a clean build is the style check.
      - name: Build
        run: dotnet build Redeclare.slnx

      - name: Test
        run: dotnet test Redeclare.slnx --no-build

      - name: Pack
        run: dotnet pack src/Redeclare/Redeclare.csproj --output artifacts/package

      # The layout tests read the nuspec. This compiles the files a consumer actually gets.
      - name: Build a consumer against the package
        run: dotnet build test/Redeclare.Consumer/Redeclare.Consumer.csproj
